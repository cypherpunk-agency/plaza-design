# Replace with your deployed contract address
VITE_CONTRACT_ADDRESS=0x...

# Set to 'true' to include registry addresses in URLs (useful for multi-network deployments)
# Default: false (registries loaded from deployments.json)
VITE_SHOW_REGISTRY_IN_URL=false
